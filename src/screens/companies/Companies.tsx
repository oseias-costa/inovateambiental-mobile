import { Text } from "react-native"
import { SafeAreaView } from "react-native"

export const Companies = () => {
    return(
        <SafeAreaView>
            <Text>Companies</Text>
        </SafeAreaView>
    )
}