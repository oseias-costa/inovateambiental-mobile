import { Text } from "react-native"
import { SafeAreaView } from "react-native"

export const Lo = () => {
    return(
        <SafeAreaView>
            <Text>Lo</Text>
        </SafeAreaView>
    )
}